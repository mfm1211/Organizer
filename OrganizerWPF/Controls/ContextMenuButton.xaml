<UserControl x:Class="OrganizerWPF.Controls.ContextMenuButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:OrganizerWPF.Controls"    
              xmlns:converters="clr-namespace:OrganizerWPF.Converters"
              x:Name="_contextMenuButton">

    <UserControl.Resources>

        
    </UserControl.Resources>
    
    <Grid DataContext="{Binding ElementName=_contextMenuButton}">
        

            <Button x:Name="MainButton"  
                    Height="23" Width="12"
                    Click="MainButton_Click" ContextMenu ="{Binding ContextMenu111}"
                    Style="{StaticResource FlatStyleNoBordersTransparentButton}">
            <Image Style="{DynamicResource threeDotsImageStyle}"/>
        </Button>


        </Grid>
</UserControl>
